import { Card, ProgressRing } from "@/components/ui";
type ProcessingStateProps = { title: string; progress?: number; indeterminate?: boolean; status?: string; showPercentage?: boolean };
export function ProcessingState({ title, progress = 0, indeterminate = false, status, showPercentage = true }: ProcessingStateProps) { return <Card className="processing-state"><ProgressRing value={progress} indeterminate={indeterminate} label={showPercentage ? undefined : title} /><div className="processing-copy"><h2 className="processing-title">{title}</h2>{status && <p className="processing-status" aria-live="polite">{status}</p>}</div></Card>; }
